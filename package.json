{
  "name": "loreweaver",
  "private": true,
  "type": "module",
  "workspaces": ["packages/core", "packages/cli"],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --force",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
