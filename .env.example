# Optional: choose the per-user Eshyra data root. When unset, Eshyra
# uses the platform default and manages campaigns through registry.json.
# ESHYRA_HOME=./.eshyra

# Advanced: bypass the managed campaign registry and open one explicit,
# unmanaged SQLite database with `eshyra play`.
# ESHYRA_DB_PATH=./campaigns/dev.db

# Claude model id used by the DM agent
ESHYRA_MODEL=claude-opus-4-7
# Provider auth for the Claude Agent SDK — set exactly one.
# An Anthropic Console API key:
ANTHROPIC_API_KEY=
# ...or a Claude Pro/Max subscription token from `claude setup-token`
# (used only when ANTHROPIC_API_KEY is unset). See docs/agent-sdk-auth.md.
# CLAUDE_CODE_OAUTH_TOKEN=

# --- Model profiles (provider-neutral) ---
# Eshyra routes tasks to capability-based profiles, each mapped to a
# provider adapter (neutral id) + model. Defaults are used when unset.
# Profiles: PREMIUM_DM, STATE_EXTRACTOR, SUMMARIZER, RULES_ADJUDICATOR,
#           MEMORY_RECONCILER, EMBEDDING_PROVIDER, ECONOMY_OR_EXPERIMENTAL
# Provider ids: anthropic, openai, bedrock, gemini, openrouter, local
# Override per profile (example shown for the primary DM profile):
# ESHYRA_PROFILE_PREMIUM_DM_PROVIDER=anthropic
# ESHYRA_PROFILE_PREMIUM_DM_MODEL=claude-opus-4-7
# Note: premium_dm enforces a capability FLOOR (Opus 4.6+/GPT-5.5-class or
# future equivalent); economy models must not silently serve the primary DM.
